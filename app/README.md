# Кадам (Kadam) — кыргызский шаг за шагом

«Кадам» — «шаг» по-кыргызски: приложение-путь изучения языка, собранный из шагов-станций.
Построено на [Astro](https://astro.build) + React-остров.

## Принцип

Уроки живут внутри приложения: **`src/content/steps/step-NN.md`** — единственный источник
контента шагов. Разговорник и словарь читаются из корня репозитория (`../phrases.md`,
`../anki/kyrgyz-frequency.csv`) без дублирования. Уроки разбиваются на темы и встраиваются
внутрь шагов пути; недельно-дневной каркас («неделя N», «дни…», цели недели, мета-секции)
отбрасывается при сборке.

## Команды

```bash
npm install
npm run serve     # собрать dist и поднять сервер: http://localhost:4321 (сайт + API)
npm run dev       # http://localhost:4322 — HMR (нужен запущенный сервер для /api)
npm run build     # статическая сборка в ./dist
npm run bot       # Telegram-бот: раз в сутки шлёт задание подписчикам (нужен TELEGRAM_BOT_TOKEN)
npm test          # серверные (ctc/static/tts/bot) + клиентские проверки
```

## Структура

```
src/
  content/steps/           # step-NN.md — источник уроков (контент шагов пути)
  content.config.ts        # glob-загрузчики: steps (локально) + phrases (из корня)
  layouts/Base.astro       # оболочка: шапка, навигация, тема
  pages/
    index.astro            # ГЛАВНАЯ = путь изучения (journey) + дневной баннер
    study.astro            # дневная сессия: смешанное FSRS-повторение + статистика (дриллы/сборка — станции пути)
    reader.astro           # чтение с кликом-переводом
    vocab.astro            # браузер словаря (CEFR-фильтр, примеры, Forvo)
  components/
    Journey.tsx            # маршрут: раскрываемые шаги с инлайн-контентом, prev/next, прогресс, deep-link #jstep-…
    StudySession.tsx       # карточки с лестницей режимов: выбор → ввод → cloze → диктант → произношение (ASR)
    Drills.tsx             # грамм-дриллы на движке морфологии (мн.ч. + 3 падежа + глагольные времена)
    SentenceBuild.tsx      # сборка предложения из банка слов (порядок SOV)
    StepExam.tsx           # экзамен-веха: смешанный мини-тест с порогом (без записи в SRS)
    StatsPanel.tsx         # прогресс: зрелые, точность, слабые слова, стрик
    DailyBanner.tsx        # план на сегодня (повторить/новых) + одна главная кнопка
    Reader.tsx             # разбор текста, словарный клик-перевод (отрезание суффиксов)
    VocabBrowser.astro     # поиск/фильтр словаря
  lib/                     # относительные импорты здесь — ТОЛЬКО с расширением `.ts`: эти модули грузит
                           # и Node (server/bot.ts), а Node ESM без расширения падает с ERR_MODULE_NOT_FOUND
    lessons.ts             # разбивка недели на темы + очистка заголовков/ссылок (pure)
    journey.ts             # сборка шагов: тема → слова → повторение → корпус (pure)
    srs.ts                 # карточки, очередь (adaptive new-limit, leech-suspend, verified-first), статистика (удержание), миграция v1→v2
    fsrs.ts                # планировщик FSRS-6 (официальные формулы и веса)
    morphology.ts          # сингармонизм таблицей суффиксов: формы + дистракторы с неверной гармонией
    sentences.ts           # курируемые фразы для сборки (порядок SOV), по шагам
    daily.ts               # дневная цель, стрик, дневной seed
    vocab.ts               # браузерная точка входа в словарь (Vite ?raw -> vocab-parse)
    vocab-parse.ts         # чистый парсер/мерж CSV-колод (общий для сайта и бота)
    remark-rewrite-links.mjs
  styles/global.css
```

## Модель пути

`lessons.ts` режет каждый шаг по `##`-темам, выкидывает мета-секции (Лексика/Задания/Чеклист),
чистит заголовки от «недели/дни». `index.astro` рендерит markdown тем в HTML через `marked` и
передаёт шаги в `Journey`. Типы шагов: урок (инлайн-контент, в фонетических уроках — встроенные
🔊), фразы выживания (step-01, из ../phrases.md), слова/повторение/корпус (StudySession),
дриллы морфологии (после шагов 2/3/4, глагольные — после 5; идут **перед** сборкой
предложений: механика → композиция), сборка предложений SOV (шаги 2–7), чтение с
кликом-переводом (шаги 10–11), экзамен-веха (шаги 4 и 8).
Практика на письмо и слух: в зрелых карточках чередуются ввод, cloze и **диктант на слух**
(TTS проигрывает слово — надо вписать). После верного ответа опционально «Тяжело»/«Легко»
(полные грейды FSRS; по умолчанию — Good, лишних кликов нет).

Фичи сомкнуты в петли: слово из читалки можно отправить «→ в повторение» (появится первым
в следующей сессии); завершение набора дриллов/фраз само отмечает станцию пути пройденной;
сданный экзамен отмечает свой шаг и хранит лучший результат. Статистика ведёт историю дней:
тепловая карта активности (12 недель) и прогноз «при текущем темпе 1500 удержанных ≈ через
X нед.». Прогресс шагов слов/корпуса считается из FSRS-данных (авто-«пройдено» при 80%
удержанных); остальное — чекбокс. Всё состояние — localStorage.

## Дневной цикл и нагрузка

Главная = единый план на сегодня: баннер показывает `повторить N · новых M · дрилл+фраза`
и одну главную кнопку («Начать сессию», пока есть работа; иначе — «Пройти новый шаг»).
Нагрузка держится устойчивой: ввод новых слов притормаживается при накоплении повторений
(`adaptiveNewLimit`), упорно забываемые слова («пиявки», ≥8 промахов) выводятся из очереди и
показываются отдельно для разбора. Прогресс шага считается по **удержанию** (слово повторено
≥2 раз и всё ещё вероятно вспоминается — `retention()` через FSRS), а не по «показано один раз».
Новые слова вводятся `verified`-первыми; непроверенный перевод помечается в карточке.

## Что сознательно НЕ делали (YAGNI)

Ни аккаунтов, ни бэкенда, ни синхронизации: состояние (FSRS-карточки, стрик, прогресс пути)
живёт в localStorage.

## Озвучка (TTS на бэкенде)

Произношение синтезирует **бэкенд** (`server/`): Meta MMS (facebook/mms-tts-kir) в ONNX через
onnxruntime-node. Браузер (`lib/tts.ts`) просто дёргает `GET /api/tts?text=…`, получает WAV и
проигрывает — модель (~114 МБ) в браузер не грузится, а повторные слова берутся из HTTP-кеша.
Синтез-хелперы (`parseTokens`/`tokenize`/`encodeWav`) — `server/tts.ts`, покрыты `test-tts.ts`.
Кнопки — в карточках, дриллах, читалке и словаре. Google TTS не поддерживает кыргызский
(проверено, 400), Forvo не встраивается без платного API — отсюда собственный MMS.

Модель (`models/tts/model.onnx` + `tokens.txt`, ~114 МБ) гитигнорена. Источник —
willwade/mms-tts-multilingual-models-onnx (папка `kir`) на HuggingFace; положить оба файла в
`models/tts/`. После переноса TTS браузер вообще не тянет `onnxruntime-web` (ASR тоже на
бэкенде) — из `dist` ушли и модель, и ORT-wasm.

## Распознавание речи + проверка произношения

Проверка произношения встроена в дневную сессию: у зрелых карточек ротацией выпадает режим
«произнеси» (`SpeakPractice.tsx`) — запись с микрофона, оценка и подсветка по буквам, результат
идёт в FSRS. Распознавание работает **на бэкенде** (`server/` — TypeScript, тот же стек:
node:http + onnxruntime-node, iarfmoose/wav2vec2-large-xlsr-kyrgyz в ONNX int8): браузер
записывает голос, чистит вход (mono + noiseSuppression/AGC, обрезка тишины — `lib/audio.ts`) и
шлёт маленький 16-кГц WAV (~64 КБ на фразу) сырым POST-body — модель (~338 МБ) в браузер
не грузится.

Оценка — по **CTC forced alignment** к целевой фразе: раз фраза известна, аудио выравнивается
(Viterbi) на ожидаемые звуки, и для каждой буквы берётся её акустическая уверенность
(Goodness-of-Pronunciation) → % + подсветка по буквам. Устойчивее нечёткого сравнения
транскрипта. CTC-математика — `server/ctc.ts`, залочена на эталонную фикстуру
(`scripts/ctc-fixture.json` ↔ `server/test-ctc.ts`).

Контракт эндпоинтов (`/api/asr/analyze`, `/api/tts`, `/api/health`) — [`server/API.md`](server/API.md).

Фронтенд (`src/`, Astro) и бэкенд (`server/`, node:http + onnxruntime-node) — **один проект**
с одним `package.json`. Запуск (один порт 4321, Node ≥23):

```
npm install
npm run fetch-models   # скачивает веса моделей (см. ниже) — один раз
npm run serve          # собирает dist и стартует сервер: http://localhost:4321 — сайт + API
```

Для разработки — `npm run dev` (:4322 с HMR); он проксирует `/api` на сервер :4321, так что
сервер (`npm start`) тоже должен быть запущен. `npm test` гоняет серверные (ctc/static/tts) и клиентские
проверки; `npm run typecheck` — оба tsconfig.

### Веса моделей

Веса (~450 МБ) гитигнорены. `npm run fetch-models` кладёт их в `models/`:
- **TTS** — готовый ONNX качается напрямую с HuggingFace (willwade/mms-tts-multilingual-models-onnx).
- **ASR** — публичного int8-ONNX нет, поэтому генерируется локально из HF-чекпоинта
  (`scripts/export-asr.py`): скрипт сам создаёт `.venv`, ставит torch и экспортирует (медленно,
  один раз). `-- --force` пере-скачивает/пере-генерирует всё.

### Конфигурация

Пути и порт настраиваются через env (реальные env-переменные приоритетнее). Скопируйте
[`.env.example`](.env.example) в `.env` — его читают и `server/main.ts`, и `fetch-models.sh`:

- `PORT` — порт сервера (по умолчанию 4321).
- `MODELS_DIR` — папка с весами (по умолчанию `<app>/models`; можно указать абсолютный путь,
  чтобы держать ~450 МБ вне репозитория).
- `STATIC_DIR` — папка собранного сайта (по умолчанию `<app>/dist`).

## Telegram-бот

Отдельный процесс (`npm run bot`, на сервере — юнит `kadam-bot`). Раз в сутки присылает сессию
из 16 упражнений: собрать предложение (порядок SOV), три дрилла на суффиксы и 12 слов дня.
Отвечать — кнопками: предложение собирается тапами по словам, суффиксы и слова — выбор из
четырёх вариантов. Неверные варианты у дриллов — тот же суффикс с нарушенной гармонией или
ассимиляцией (`үйгө` против `үйкө`, `үйга`, `үйге`), то есть проверяется ровно правило урока.
В конце — итог «N из 16» с разбором того, что не получилось.

Контент берётся из тех же модулей, что и сайт, и от того же `daySeed()`. Сессия хранит только
курсор — день, номер упражнения и список промахов (счёт выводится из них); сами упражнения пересобираются из сида, поэтому
переживают рестарт. Личный прогресс бот не знает: он живёт в localStorage браузера.

Команды: `/start` — подписаться и сразу получить задание, `/task` — вернуться к сегодняшнему,
`/stop` — отписаться. Всё остальное игнорируется.

Состояние (подписчики, их сессии, offset) — один JSON-файл, запись атомарная. В продакшене он
лежит в `/var/lib/kadam/`, а не рядом с приложением: `deploy.sh` выполняет `rsync --delete`
в `/opt/kadam`, и файл внутри этого дерева стирался бы при каждом деплое.

Настройки — `TELEGRAM_BOT_TOKEN`, `TELEGRAM_SEND_AT`, `TELEGRAM_STATE_FILE` (см. `.env.example`).
