node_modules/
dist/
.astro/

# Large ONNX model weights — served locally, exceed GitHub's 100 MB file limit.
# Regenerate the ASR model with scripts/export-asr.py (see web/README.md).
public/**/model.onnx
